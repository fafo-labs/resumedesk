import localFont from 'next/font/local';

export const ClashGrotesk = localFont({
  src: './ClashGrotesk-Variable.ttf',
  variable: '--local-clashgrotesk',
});


export const localDepartureMono = localFont({
  src: "./DepartureMono-Regular.otf",
  variable: "--local-departure-mono",
});